/** @type {import('next').NextConfig} */
const nextConfig = {
  //   reactStrictMode: true,
  //   swcMinification: true,
  images: {
    domains: ["github.com"],
  },
};

export default nextConfig;

